.messages#fade
    each type in Object.keys(messages)
        each message in messages[type]
            div(class=`alert alert-${type}`, style="position: fixed; bottom: 30px; right: 40px; z-index: 99999;width: 30%;") 
                strong #{message}
            